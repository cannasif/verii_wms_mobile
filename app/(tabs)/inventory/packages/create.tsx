import React from 'react';
import { PackageCreateScreen } from '@/features/package-mobile/screens/PackageCreateScreen';

export default function PackageCreateRoute(): React.ReactElement {
  return <PackageCreateScreen />;
}
